# Firebase Services

This folder will contain Flutter Firebase support services such as notifications, analytics, crash reporting, and remote config if selected.

Firebase must remain a supporting service and should not replace the PHP and MySQL backend.
